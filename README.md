Web app for processing Cisco ASA firewalls config files.

Takes a config file, parses it and hierarchically displays:
 - network objects and groups,
 - users and user groups,
 - routes,
 - NAT rules,
 - public services,
 - access control lists.
 